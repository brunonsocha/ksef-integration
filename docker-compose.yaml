services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ksef-integration
    ports:
      - "8080:8080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./data:/app/data
    working_dir: /app
    restart: unless-stopped
